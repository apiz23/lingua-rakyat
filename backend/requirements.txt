# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt - Lingua Rakyat Backend Dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: All LangChain packages must be from the same generation (>=1.0).
# Mixing old (0.2.x) and new (1.x) versions causes dependency conflicts.
# ─────────────────────────────────────────────────────────────────────────────

# Web Framework & Server
fastapi==0.111.0
uvicorn[standard]
python-multipart

# PDF Processing
pypdf>=4.0.0

# LangChain Core Stack (all 1.x generation for compatibility)
langchain==1.2.12
langchain-community==0.4.1
langchain-core==1.2.19
langchain-text-splitters==1.1.1

# LLM Integrations
langchain-groq==1.1.2
langchain-pinecone>=0.2.0

# Vector Database
pinecone-client>=7.0.0

# File Storage
supabase>=2.28.0

# Embeddings (runs locally)
sentence-transformers>=2.2.0

# Language Detection
langdetect==1.0.9

# Environment Management
python-dotenv>=1.0.0

# Utilities
requests>=2.31.0
